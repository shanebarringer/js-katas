/* Write a function named last that returns the last value given. 
    For example:
      last( [1,2,3,4] ) // => 4
      last( 1,2,3,4 )   // => 4
*/

const last = () => {
  // solution here
};

module.exports = last;
